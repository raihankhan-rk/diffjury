# TypeSafe API key for Jev (server-only — never expose to the browser)
TYPESAFE_API_KEY=

# Optional GitHub token for higher PR-fetch rate limits (server-only)
# Unauthenticated public API works; token raises the limit.
# Never commit a real token.
GITHUB_TOKEN=
